# 智能 Agent 项目技术方案（v2）

> 版本：v0.2 | 日期：2026-05-07 | 状态：待 Review

---

## 一、项目概述

### 1.1 项目目标

构建一个基于 LLM 的智能代理系统，具备以下核心能力：

- 基于小米 MiMo 大模型的智能对话与任务执行
- 专业知识库 RAG 增强检索（支持文件上传 + 网址抓取）
- 自主决策的工具调度（丰富的内置工具集）
- 可持久化的双层记忆系统（短期/长期）
- 情绪判断与情感反馈
- 语音合成（TTS）输出
- Skills 技能系统（使用 + 生成）
- MCP 协议支持（外部工具扩展）
- 管理中台界面（知识库管理、技能管理、会话管理等）
- 完整的前后端界面

### 1.2 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                    前端 (React + TS)                      │
│              agent-demo-web/                             │
│    对话界面 · 语音播放 · 情绪展示 · 管理中台               │
└──────────────────────┬──────────────────────────────────┘
                       │ HTTP / SSE
┌──────────────────────┴──────────────────────────────────┐
│                   后端 (FastAPI)                          │
│              agent-demo-server/                           │
│    API 路由 · 会话管理 · SQLAdmin 管理面板                 │
└──────────────────────┬──────────────────────────────────┘
                       │ Python 内部调用
┌──────────────────────┴──────────────────────────────────┐
│                  Agent 核心 (LangGraph)                   │
│              agent-demo-agent/                            │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌─────────────┐ │
│  │ Agent 图  │ │  RAG 引擎 │ │ 工具中心  │ │  记忆系统    │ │
│  │ (Graph)   │ │ (Retriever)│ │ (Tools)  │ │ (Memory)    │ │
│  └──────────┘ └──────────┘ └──────────┘ └─────────────┘ │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌─────────────┐ │
│  │ 情绪分析  │ │ TTS 语音  │ │ Skills   │ │ MCP 客户端   │ │
│  └──────────┘ └──────────┘ └──────────┘ └─────────────┘ │
└─────────────────────────────────────────────────────────┘
                       │
        ┌──────────────┼──────────────┐
        ▼              ▼              ▼
  ┌──────────┐  ┌──────────┐  ┌──────────┐
  │ MiMo API │  │ ChromaDB │  │ SQLite/  │
  │ (LLM+TTS)│  │ (向量库)  │  │PostgreSQL│
  └──────────┘  └──────────┘  └──────────┘
```

---

## 二、技术选型

### 2.1 核心技术栈

| 层级 | 技术选型 | 说明 |
|------|---------|------|
| **Agent 框架** | LangGraph | LangChain 官方 Agent 编排框架，替代已废弃的 AgentExecutor |
| **LLM 模型** | 小米 MiMo v2.5-pro | OpenAI 兼容 API，支持 Function Calling、结构化输出 |
| **向量数据库** | Chroma（开发）/ Qdrant（生产） | 开发阶段 Chroma 零配置，生产可切 Qdrant |
| **嵌入模型** | BAAI/bge-base-zh-v1.5（本地） | 中文效果最佳的本地 embedding 模型，约 400MB |
| **短期记忆** | LangGraph Checkpointer + SQLite | 会话级别记忆，线程隔离 |
| **长期记忆** | LangGraph Store + langmem | 跨会话持久化，自动事实提取 |
| **TTS 语音** | MiMo TTS（主）/ Edge-TTS（备） | MiMo TTS 限时免费优先尝试，Edge-TTS 免费且中文质量高 |
| **后端框架** | FastAPI + SSE + SQLAdmin | 异步高性能，流式输出 + 管理面板 |
| **前端框架** | React + TypeScript + Vite + Ant Design | 简洁风 UI |
| **MCP 支持** | langchain-mcp-adapters | 官方 MCP 适配器，支持 stdio/SSE/HTTP 传输 |
| **包管理** | uv（Python）/ pnpm（前端） | 快速、可靠的依赖管理 |

### 2.2 MiMo 模型详情

| 项目 | 值 |
|------|---|
| OpenAI 兼容 Base URL | `https://api.xiaomimimo.com/v1` |
| 认证方式 | 尝试标准 Bearer → 如失败则自定义 `api-key` 头 |
| 推荐模型 | `mimo-v2.5-pro`（主力）/ `mimo-v2-flash`（轻量快速） |
| 上下文窗口 | 最高 1M tokens（pro）/ 256K（flash） |
| 支持能力 | Function Calling、结构化输出、深度思考、联网搜索 |
| TTS 模型 | `mimo-v2.5-tts`（目前限时免费） |

**兼容性策略：** 优先使用 LangChain `ChatOpenAI` 直连。若认证不兼容，封装自定义 `MimoChatProvider`，通过 `httpx` 直接调用 API 并适配 LangChain 接口。

### 2.3 TTS 方案

**主选：MiMo TTS**（`mimo-v2.5-tts`，限时免费，与 LLM 同平台）
**备选：Edge-TTS**（零成本、微软 Neural 引擎、中文 `zh-CN-XiaoxiaoNeural` 质量极高）

两者都通过统一的 `TTSEngine` 抽象接口调用，可随时切换。

### 2.4 嵌入模型

**选用：`BAAI/bge-base-zh-v1.5`**

- 中文 MTEB 排行榜表现最佳的轻量模型之一
- 768 维，约 400MB，CPU 可运行
- 首次运行自动从 HuggingFace 下载
- 通过 `langchain-community` 的 `HuggingFaceBgeEmbeddings` 封装

---

## 三、项目结构

三个独立子项目，物理隔离，各自独立部署：

```
agentDemo/
├── 技术方案.md                        # 本文档
├── docker-compose.yml                 # 一键启动（可选）
│
├── agent-demo-agent/                  # Agent 核心层
│   ├── pyproject.toml
│   ├── .env.example
│   ├── src/
│   │   └── agent/
│   │       ├── __init__.py
│   │       ├── main.py                # Agent 入口 & 快速启动
│   │       ├── config.py              # pydantic-settings 配置
│   │       ├── graph.py               # LangGraph StateGraph 定义
│   │       ├── nodes.py               # 图节点：思考、工具调用、情绪分析
│   │       ├── state.py               # AgentState TypedDict
│   │       ├── prompts.py             # System prompt 模板
│   │       │
│   │       ├── tools/                 # 工具中心
│   │       │   ├── __init__.py
│   │       │   ├── registry.py        # 工具注册中心（动态注册/发现）
│   │       │   ├── search.py          # DuckDuckGo 网络搜索
│   │       │   ├── wikipedia.py       # Wikipedia 查询
│   │       │   ├── web_scraper.py     # 网页内容抓取（URL → 文本）
│   │       │   ├── calculator.py      # 数学计算
│   │       │   ├── datetime_tool.py   # 日期时间工具
│   │       │   ├── file_ops.py        # 文件读写操作
│   │       │   ├── shell.py           # Shell 命令执行
│   │       │   ├── python_repl.py     # Python 代码执行
│   │       │   ├── json_tool.py       # JSON 解析/查询
│   │       │   ├── csv_tool.py        # CSV 读取/查询
│   │       │   ├── http_tool.py       # HTTP 请求工具
│   │       │   └── url_to_knowledge.py # URL → 知识库入库工具
│   │       │
│   │       ├── skills/                # Skills 技能系统
│   │       │   ├── __init__.py
│   │       │   ├── registry.py        # 技能注册中心
│   │       │   ├── executor.py        # 技能执行器
│   │       │   ├── generator.py       # LLM 自动生成新技能
│   │       │   ├── builtin/           # 内置技能
│   │       │   │   ├── research.py    # 研究助手技能
│   │       │   │   ├── coding.py      # 编程助手技能
│   │       │   │   ├── data_analyst.py # 数据分析技能
│   │       │   │   └── writer.py      # 写作助手技能
│   │       │   └── store/             # 生成的技能持久化
│   │       │       └── .gitkeep
│   │       │
│   │       ├── rag/                   # RAG 知识库
│   │       │   ├── __init__.py
│   │       │   ├── loader.py          # 文档加载器（PDF/Word/MD/TXT/URL）
│   │       │   ├── splitter.py        # 文本分割
│   │       │   ├── embedder.py        # 嵌入模型封装（bge-base-zh）
│   │       │   ├── retriever.py       # 检索器（封装为 Agent Tool）
│   │       │   └── ingest.py          # 知识库入库脚本
│   │       │
│   │       ├── memory/                # 记忆系统
│   │       │   ├── __init__.py
│   │       │   ├── short_term.py      # 短期记忆（Checkpointer + SQLite）
│   │       │   ├── long_term.py       # 长期记忆（Store + langmem）
│   │       │   └── manager.py         # 记忆管理器统一接口
│   │       │
│   │       ├── emotion/               # 情绪系统
│   │       │   ├── __init__.py
│   │       │   ├── analyzer.py        # 情绪分析节点
│   │       │   └── feedback.py        # 情绪反馈策略
│   │       │
│   │       ├── tts/                   # TTS 语音
│   │       │   ├── __init__.py
│   │       │   ├── engine.py          # TTS 引擎抽象层
│   │       │   ├── mimo_tts.py        # MiMo TTS 实现（主选）
│   │       │   └── edge_tts.py        # Edge-TTS 实现（备选）
│   │       │
│   │       └── mcp/                   # MCP 协议支持
│   │           ├── __init__.py
│   │           ├── client.py          # MCP 客户端封装
│   │           └── config.py          # MCP 服务器配置
│   │
│   ├── knowledge/                     # 知识库源文件目录
│   │   └── .gitkeep
│   ├── mcp_servers/                   # 本地 MCP 服务器配置
│   │   └── config.json
│   ├── tests/
│   │   ├── test_graph.py
│   │   ├── test_rag.py
│   │   ├── test_memory.py
│   │   ├── test_emotion.py
│   │   ├── test_tools.py
│   │   └── test_skills.py
│   └── README.md
│
├── agent-demo-server/                 # 后端服务层
│   ├── pyproject.toml
│   ├── .env.example
│   ├── src/
│   │   └── server/
│   │       ├── __init__.py
│   │       ├── main.py                # FastAPI 应用入口
│   │       ├── config.py              # 服务配置
│   │       ├── deps.py                # 依赖注入（Agent 实例、DB 连接）
│   │       ├── api/
│   │       │   ├── __init__.py
│   │       │   ├── chat.py            # 对话 API（/chat, /chat/stream）
│   │       │   ├── knowledge.py       # 知识库管理 API（文件上传 + URL 入库）
│   │       │   ├── skills.py          # 技能管理 API
│   │       │   ├── memory.py          # 记忆查询/管理 API
│   │       │   ├── tools.py           # 工具列表/状态 API
│   │       │   ├── tts.py             # 语音合成 API
│   │       │   └── health.py          # 健康检查
│   │       ├── models/
│   │       │   ├── __init__.py
│   │       │   ├── chat.py            # 请求/响应 Pydantic 模型
│   │       │   ├── knowledge.py       # 知识库相关模型
│   │       │   └── skill.py           # 技能相关模型
│   │       ├── db/
│   │       │   ├── __init__.py
│   │       │   ├── database.py        # 数据库连接（SQLAlchemy）
│   │       │   └── models.py          # ORM 模型（会话、知识库、技能、用户预留）
│   │       ├── admin/
│   │       │   ├── __init__.py
│   │       │   └── views.py           # SQLAdmin 管理面板视图
│   │       └── middleware/
│   │           ├── __init__.py
│   │           └── cors.py            # CORS 配置
│   ├── tests/
│   └── README.md
│
└── agent-demo-web/                    # 前端界面层
    ├── package.json
    ├── tsconfig.json
    ├── vite.config.ts
    ├── index.html
    ├── src/
    │   ├── main.tsx
    │   ├── App.tsx
    │   ├── api/                       # API 请求封装
    │   │   ├── client.ts              # axios/http 封装
    │   │   ├── chat.ts                # 对话 API
    │   │   ├── knowledge.ts           # 知识库 API
    │   │   ├── skills.ts              # 技能 API
    │   │   └── tts.ts                 # TTS API
    │   ├── components/
    │   │   ├── ChatBox.tsx            # 对话主组件
    │   │   ├── MessageBubble.tsx      # 消息气泡（Markdown 渲染）
    │   │   ├── VoicePlayer.tsx        # 语音播放组件
    │   │   ├── EmotionBadge.tsx       # 情绪标识
    │   │   ├── ToolCallCard.tsx       # 工具调用展示卡片
    │   │   ├── Sidebar.tsx            # 侧边栏（会话列表）
    │   │   └── InputBar.tsx           # 输入栏（文本 + 语音 + 文件上传）
    │   ├── pages/
    │   │   ├── Home.tsx               # 对话主页面
    │   │   ├── Knowledge.tsx          # 知识库管理页面
    │   │   ├── Skills.tsx             # 技能管理页面
    │   │   ├── Memory.tsx             # 记忆查看页面
    │   │   └── Settings.tsx           # 设置页面
    │   ├── hooks/
    │   │   ├── useChat.ts
    │   │   └── useSSE.ts
    │   ├── stores/                    # 状态管理（Zustand）
    │   │   ├── chatStore.ts
    │   │   └── appStore.ts
    │   ├── styles/
    │   │   └── global.css
    │   └── types/
    │       └── index.ts
    ├── public/
    ├── tests/
    └── README.md
```

---

## 四、核心模块设计

### 4.1 Agent 图（LangGraph）

采用 LangGraph 的 `create_react_agent` 快速启动，后续可迁移至自定义 `StateGraph`。

```
              ┌──────────┐
              │  __start__ │
              └─────┬────┘
                    ▼
              ┌──────────┐
              │   思考节点  │  ← LLM 分析用户意图
              └─────┬────┘
                    ▼
             ┌──────┴──────┐
             │  路由判断     │  ← Function Calling 自动决策
             └──┬───┬───┬──┘
                │   │   │
        ┌───────┘   │   └───────┐
        ▼           ▼           ▼
  ┌──────────┐ ┌──────────┐ ┌──────────┐
  │ 工具调用   │ │ Skills   │ │ RAG 检索  │
  │ (Tools)   │ │ 执行     │ │ (Retriever)│
  └─────┬────┘ └─────┬────┘ └─────┬────┘
        │           │           │
        └─────┬─────┴───────────┘
              ▼
        ┌──────────┐
        │ 情绪分析   │  ← LLM 结构化输出判断情绪
        └─────┬────┘
              ▼
        ┌──────────┐
        │   输出     │  ← 组装回复 + 情绪标签 + TTS
        └──────────┘
```

**状态定义：**

```python
class AgentState(TypedDict):
    messages: Annotated[list[BaseMessage], add_messages]
    emotion: str                          # 当前情绪标签
    tools_used: list[str]                 # 本轮使用的工具
    active_skill: str | None              # 当前激活的技能
    rag_context: str                      # RAG 检索到的上下文
    should_use_tts: bool                  # 是否需要语音输出
```

### 4.2 工具中心（Tools）

内置丰富的免费工具集，全部无需 API Key：

| 工具 | 说明 | 依赖包 |
|------|------|--------|
| DuckDuckGo 搜索 | 网络搜索 | `duckduckgo-search` |
| Wikipedia 查询 | 百科查询 | `wikipedia` |
| 网页抓取 | URL → 文本内容 | `beautifulsoup4` + `httpx` |
| URL → 知识库 | 抓取网页并入库 | 自研（基于网页抓取 + RAG） |
| 数学计算 | 安全的数学表达式求值 | `numexpr` |
| 日期时间 | 获取当前日期时间 | 标准库 |
| 文件操作 | 读写/列出/移动文件 | `langchain-community` |
| Shell 执行 | 执行 Shell 命令 | `langchain-community` |
| Python REPL | 执行 Python 代码 | `langchain-experimental` |
| JSON 工具 | JSON 解析与查询 | `langchain-community` |
| CSV 工具 | CSV 读取与查询 | `langchain-community` |
| HTTP 请求 | 发送 HTTP 请求 | `requests` |

**工具注册中心：** 所有工具通过 `ToolRegistry` 统一注册管理，支持动态启用/禁用。

### 4.3 Skills 技能系统

Skills 是工具 + 提示词 + 工作流的组合，提供更专业的场景能力。

**三层架构：**

```
┌─────────────────────────────────────┐
│          Skills 技能系统              │
├─────────────┬───────────────────────┤
│  内置技能    │    动态生成技能         │
│  (Builtin)  │    (Generated)        │
├─────────────┼───────────────────────┤
│ • 研究助手   │  用户描述需求 →        │
│ • 编程助手   │  LLM 生成 Python 函数  │
│ • 数据分析   │  → 验证 → 注册为新技能  │
│ • 写作助手   │  → 持久化到磁盘        │
└─────────────┴───────────────────────┘
```

**技能数据结构：**

```python
class Skill:
    name: str                    # 技能名称
    description: str             # 技能描述
    tools: list[BaseTool]        # 该技能使用的工具列表
    system_prompt: str           # 技能专用系统提示词
    is_builtin: bool             # 是否内置
    created_at: datetime         # 创建时间
```

**技能生成流程（LLM 生成）：**
1. 用户描述需要的技能（如"帮我做一个汇率转换技能"）
2. LLM 生成对应的 Python 工具函数 + 系统提示词
3. 自动测试生成的代码
4. 注册到技能中心并持久化

### 4.4 MCP 协议支持

通过 `langchain-mcp-adapters` 将外部 MCP 服务器的工具无缝接入 Agent。

**支持的传输方式：**
- `stdio`：本地子进程（如本地 MCP 工具服务器）
- `streamable_http`：远程 HTTP 服务
- `sse`：Server-Sent Events（旧版兼容）

**配置文件 `mcp_servers/config.json`：**

```json
{
  "filesystem": {
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-filesystem", "/path/to/dir"],
    "transport": "stdio"
  },
  "custom_api": {
    "url": "http://localhost:9000/mcp",
    "transport": "streamable_http"
  }
}
```

**使用方式：** Agent 启动时自动连接配置的 MCP 服务器，MCP 工具与内置工具统一管理。

### 4.5 RAG 知识库增强

**支持的知识源：**
- 文件上传：PDF、Word（.docx）、Markdown、TXT
- URL 网址：自动抓取网页内容 → 清洗 → 入库
- 手动录入：直接输入文本

**入库流程：**

```
文件/URL/文本 → 文档加载器 → 文本分割 → bge-base-zh 向量化 → ChromaDB 存储
                    ↑
              URL: httpx 抓取 → BeautifulSoup 清洗 → 提取正文
```

**检索策略：** MMR（Maximal Marginal Relevance），平衡相关性与多样性

### 4.6 双层记忆系统

| 维度 | 短期记忆 (STM) | 长期记忆 (LTM) |
|------|---------------|---------------|
| 内容 | 当前会话上下文、最近对话 | 跨会话用户偏好、学习到的事实 |
| 实现 | LangGraph Checkpointer + SQLite | LangGraph Store + langmem |
| 生命周期 | 会话级，自动过期 | 永久，定期归纳整理 |
| 隔离 | 按 thread_id 隔离 | 按 namespace（用户ID）隔离 |

### 4.7 情绪分析与反馈

LLM 结构化输出判断用户情绪，根据情绪调整回复风格：

| 用户情绪 | Agent 回复风格 |
|---------|---------------|
| happy | 积极回应，适当轻松语气 |
| sad | 共情安慰，温和语气 |
| angry | 冷静回应，承认问题，提供方案 |
| anxious | 耐心解释，提供确定性信息 |
| confused | 逐步引导，简单语言 |
| neutral | 正常专业回复 |
| excited | 同频回应，支持想法 |

### 4.8 TTS 语音合成

**统一抽象接口：**

```python
class TTSEngine(ABC):
    @abstractmethod
    async def synthesize(self, text: str, voice: str = "default") -> bytes: ...
```

**主选 MiMo TTS：** 调用 `mimo-v2.5-tts` API，与 LLM 同平台
**备选 Edge-TTS：** 本地调用，中文 `zh-CN-XiaoxiaoNeural` 质量极高

### 4.9 管理中台

**方案：FastAPI SQLAdmin + 前端管理页面**

- `/admin` — SQLAdmin 后台（直接操作数据库：会话记录、知识库文档、技能列表、用户管理预留）
- `/docs` — Swagger API 文档（开发者调试用）
- 前端 `/knowledge` — 知识库管理（上传文件、输入 URL、查看已入库文档、删除）
- 前端 `/skills` — 技能管理（查看、启用/禁用、生成新技能）
- 前端 `/memory` — 记忆查看（短期/长期记忆浏览）
- 前端 `/settings` — 系统设置

---

## 五、API 接口设计

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/chat` | 同步对话 |
| POST | `/api/chat/stream` | 流式对话（SSE） |
| GET | `/api/chat/history/{thread_id}` | 获取会话历史 |
| POST | `/api/knowledge/upload` | 上传文件到知识库 |
| POST | `/api/knowledge/ingest-url` | URL 抓取并入库 |
| POST | `/api/knowledge/ingest-text` | 文本直接入库 |
| GET | `/api/knowledge/list` | 列出已入库文档 |
| DELETE | `/api/knowledge/{doc_id}` | 删除知识库文档 |
| GET | `/api/skills/list` | 列出所有技能 |
| POST | `/api/skills/create` | 创建新技能（手动） |
| POST | `/api/skills/generate` | LLM 自动生成技能 |
| PUT | `/api/skills/{skill_id}` | 更新技能 |
| DELETE | `/api/skills/{skill_id}` | 删除技能 |
| GET | `/api/tools/list` | 列出所有可用工具 |
| PUT | `/api/tools/{tool_name}/toggle` | 启用/禁用工具 |
| GET | `/api/memory/{thread_id}` | 查询会话记忆 |
| GET | `/api/memory/long-term/{user_id}` | 查询长期记忆 |
| POST | `/api/tts` | 文本转语音 |
| GET | `/api/health` | 健康检查 |

---

## 六、用户认证预留设计

初版不实现用户认证，但核心设计预留扩展点：

```python
# deps.py - 依赖注入中预留用户上下文
async def get_current_user(request: Request) -> UserContext:
    """初版返回默认用户，后续接入认证后替换此函数"""
    # TODO: 从 JWT/Session 中解析用户
    return UserContext(user_id="default", username="default")

# API 路由中使用
@router.post("/api/chat")
async def chat(request: ChatRequest, user: UserContext = Depends(get_current_user)):
    thread_id = f"{user.user_id}_{request.thread_id}"
    ...
```

- 所有 API 路由通过 `Depends(get_current_user)` 注入用户上下文
- 记忆系统按 `user_id` 隔离
- 数据库模型预留 `user_id` 字段
- 后续只需修改 `get_current_user` 函数即可接入 JWT/OAuth

---

## 七、环境准备与搭建步骤

### 7.1 环境检测与安装

```bash
# ===== 前置条件检查 =====

# Python 3.11+
python3 --version || echo "请安装 Python 3.11+"

# Node.js 18+
node --version || echo "请安装 Node.js 18+"

# pnpm（前端包管理器）
pnpm --version || npm install -g pnpm

# uv（Python 包管理器）
uv --version || curl -LsSf https://astral.sh/uv/install.sh | sh

# Git
git --version || echo "请安装 Git"
```

### 7.2 环境变量配置

各子项目 `.env` 文件：

```env
# ===== agent-demo-agent/.env =====
# MiMo API
MIMO_API_KEY=your_api_key_here
MIMO_BASE_URL=https://api.xiaomimimo.com/v1
MIMO_MODEL=mimo-v2.5-pro
MIMO_MODEL_FAST=mimo-v2-flash

# TTS
TTS_ENGINE=mimo-tts
TTS_MIMO_MODEL=mimo-v2.5-tts
TTS_FALLBACK_ENGINE=edge-tts
TTS_VOICE=zh-CN-XiaoxiaoNeural

# 向量库：开发默认 Chroma，生产推荐 Qdrant
VECTOR_STORE=chroma
CHROMA_PERSIST_DIR=./data/chroma
QDRANT_URL=http://localhost:6333
QDRANT_API_KEY=
QDRANT_COLLECTION_PREFIX=agent_demo
QDRANT_VECTOR_SIZE=768

# 嵌入模型
EMBEDDING_MODEL=BAAI/bge-base-zh-v1.5

# 记忆
SHORT_TERM_DB=./data/memory.db
LONG_TERM_DB=./data/long_term.db

# MCP
MCP_CONFIG_PATH=./mcp_servers/config.json
```

```env
# ===== agent-demo-server/.env =====
# 服务
SERVER_HOST=0.0.0.0
SERVER_PORT=8000

# 数据库
DATABASE_URL=sqlite:///./data/server.db

# Agent（指向 agent 项目路径或 API）
AGENT_MODULE_PATH=../agent-demo-agent
```

### 7.3 创建 Agent 核心项目

```bash
cd /path/to/agentDemo

# 创建项目
mkdir -p agent-demo-agent/src/agent
cd agent-demo-agent

# 初始化
uv init --python 3.11

# 核心依赖
uv add \
    langgraph \
    langchain-core \
    langchain-openai \
    langchain-community \
    langchain-experimental \
    langchain-text-splitters \
    langchain-mcp-adapters \
    langmem \
    chromadb \
    sentence-transformers \
    pydantic-settings \
    python-dotenv \
    uvicorn

# TTS 依赖
uv add edge-tts httpx

# 工具依赖
uv add duckduckgo-search wikipedia beautifulsoup4 numexpr requests

# 开发依赖
uv add --dev pytest pytest-asyncio ruff

# 创建目录结构
mkdir -p src/agent/{tools,skills/builtin,skills/store,rag,memory,emotion,tts,mcp}
mkdir -p knowledge tests data mcp_servers

# 创建 __init__.py
for d in src/agent src/agent/tools src/agent/skills src/agent/rag \
         src/agent/memory src/agent/emotion src/agent/tts src/agent/mcp; do
    touch "$d/__init__.py"
done
```

### 7.4 创建后端服务项目

```bash
cd /path/to/agentDemo

mkdir -p agent-demo-server/src/server
cd agent-demo-server

uv init --python 3.11

# 依赖
uv add \
    fastapi \
    uvicorn[standard] \
    sse-starlette \
    python-multipart \
    pydantic-settings \
    python-dotenv \
    httpx \
    sqlalchemy \
    aiosqlite \
    sqladmin

# 开发依赖
uv add --dev pytest pytest-asyncio httpx ruff

# 创建目录结构
mkdir -p src/server/{api,models,db,admin,middleware}
mkdir -p tests data

# 创建 __init__.py
for d in src/server src/server/api src/server/models src/server/db \
         src/server/admin src/server/middleware; do
    touch "$d/__init__.py"
done
```

### 7.5 创建前端项目

```bash
cd /path/to/agentDemo

pnpm create vite agent-demo-web --template react-ts
cd agent-demo-web

# 依赖
pnpm add zustand antd @ant-design/icons react-markdown remark-gfm

# 开发依赖
pnpm add -D @types/react @types/react-dom eslint prettier

# 创建目录结构
mkdir -p src/{api,components,pages,hooks,stores,styles,types}
```

### 7.6 Docker 部署（可选）

```yaml
# docker-compose.yml
version: "3.8"
services:
  agent:
    build: ./agent-demo-agent
    ports:
      - "8001:8001"
    volumes:
      - ./agent-demo-agent/data:/app/data
      - ./agent-demo-agent/knowledge:/app/knowledge
    env_file:
      - ./agent-demo-agent/.env

  server:
    build: ./agent-demo-server
    ports:
      - "8000:8000"
    depends_on:
      - agent
    volumes:
      - ./agent-demo-server/data:/app/data
    env_file:
      - ./agent-demo-server/.env

  web:
    build: ./agent-demo-web
    ports:
      - "3000:3000"
    depends_on:
      - server
```

---

## 八、开发路线图

### Phase 1：基础框架（第 1-2 周）

- [ ] 搭建三个子项目骨架，配置依赖
- [ ] 验证 MiMo API + LangChain ChatOpenAI 连通性
- [ ] 实现基础 Agent 图（create_react_agent + 最少工具）
- [ ] 实现 FastAPI 基础路由 + SSE 流式输出
- [ ] 实现 React 基础对话界面

### Phase 2：核心能力（第 3-4 周）

- [ ] 实现完整工具中心（所有内置工具）
- [ ] 实现 RAG 知识库（文件上传 + URL 抓取 + 向量化 + 检索）
- [ ] 实现短期记忆 + 长期记忆
- [ ] 实现 MCP 客户端集成

### Phase 3：增强功能（第 5-6 周）

- [ ] 实现 Skills 技能系统（内置技能 + LLM 生成技能）
- [ ] 实现情绪分析与反馈策略
- [ ] 集成 TTS（MiMo TTS + Edge-TTS 备选）
- [ ] 前端增加语音播放、情绪标识、工具调用展示

### Phase 4：工程化（第 7-8 周）

- [ ] 实现管理中台（SQLAdmin + 前端管理页面）
- [ ] 用户认证预留设计落地
- [ ] Docker 容器化
- [ ] 单元测试、集成测试
- [ ] 日志、监控、错误处理

---

## 九、关键依赖清单

### Agent 核心

```toml
[project]
dependencies = [
    "langgraph>=0.2",
    "langchain-core>=0.3",
    "langchain-openai>=0.2",
    "langchain-community>=0.3",
    "langchain-experimental>=0.3",
    "langchain-text-splitters>=0.3",
    "langchain-mcp-adapters>=0.1",
    "langmem>=0.1",
    "chromadb>=0.5",
    "sentence-transformers>=3.0",
    "edge-tts>=6.1",
    "pydantic-settings>=2.0",
    "python-dotenv>=1.0",
    "duckduckgo-search>=6.0",
    "wikipedia>=1.4",
    "beautifulsoup4>=4.12",
    "numexpr>=2.10",
    "requests>=2.31",
    "httpx>=0.27",
]
```

### 后端服务

```toml
[project]
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "sse-starlette>=2.0",
    "python-multipart>=0.0.9",
    "pydantic-settings>=2.0",
    "python-dotenv>=1.0",
    "httpx>=0.27",
    "sqlalchemy>=2.0",
    "aiosqlite>=0.20",
    "sqladmin>=0.19",
]
```

### 前端

```json
{
  "dependencies": {
    "react": "^19.0",
    "react-dom": "^19.0",
    "zustand": "^4.5",
    "antd": "^5.22",
    "@ant-design/icons": "^5.5",
    "react-markdown": "^9.0",
    "remark-gfm": "^4.0"
  }
}
```

---

## 十、风险与注意事项

| 风险 | 影响 | 缓解措施 |
|------|------|---------|
| MiMo API 不兼容 ChatOpenAI 认证格式 | Agent 无法调用 | 封装自定义 `MimoChatProvider`，httpx 直接调用 |
| MiMo TTS 质量不佳或到期收费 | TTS 功能受影响 | 自动降级到 Edge-TTS（已验证免费且质量高） |
| LangGraph 版本迭代快 | API 变更 | 锁定版本号，关注 changelog |
| bge 模型首次下载慢 | 首次启动延迟 | 提前下载到本地，Docker 构建时预下载 |
| RAG 检索质量 | 知识库回答不准确 | 调优 chunk_size、检索策略，增加 RAG 评估 |
| 长期记忆数据膨胀 | 性能下降 | 定期整理、设置存储上限、过期清理 |
| LLM 生成的技能代码安全 | 代码注入风险 | 沙箱执行、代码审查、禁止危险操作 |
