"""
提示词模板模块

集中管理所有发送给 LLM 的提示词模板。
使用 Python 字符串的 .format() 方法插入动态变量。

模板变量说明：
- {rag_context}: RAG 知识库检索到的内容，如果知识库为空则为空字符串
- {message}: 用户的原始消息
- {description}: 用户描述的技能需求
"""

# ===== 系统提示词 =====
# 这是 Agent 的"人设"，定义了它的能力、行为规则和回复风格
# {rag_context} 占位符会在运行时被替换为知识库检索结果
SYSTEM_PROMPT = """你是一个智能助手，具备以下能力：

1. **工具使用**：你可以使用搜索、计算、文件操作、网页抓取等多种工具来完成任务
2. **知识检索**：当用户的问题涉及已有知识库内容时，优先检索知识库
3. **情绪感知**：我会分析用户的情绪状态，并相应调整回复风格
4. **技能系统**：对于复杂任务，可以调用专门的技能来处理

回复规则：
- 使用中文回复
- 回答要简洁明了，避免冗长
- 如果不确定，诚实说明
- 使用 Markdown 格式增强可读性
- 根据用户情绪调整语气

{memory_context}
{rag_context}
{skill_context}
"""

# ===== 情绪分析提示词 =====
# 让 LLM 分析用户消息的情绪，返回一个标签词
# 使用结构化输出约束，只返回预定义的情绪标签
EMOTION_PROMPT = """分析以下用户消息的情绪状态，只返回一个情绪标签词。

用户消息：{message}

可选标签：happy, sad, angry, anxious, confused, neutral, excited

只返回标签，不要其他内容。"""

# ===== 技能生成提示词 =====
# 当用户要求创建新技能时，让 LLM 生成 Markdown 格式的 Skill 规范文档
SKILL_GENERATION_PROMPT = """你是一个技能生成器。根据用户的需求描述，生成一个 Markdown 格式的 Skill 规范文档。

用户需求：{description}

要求：
1. 使用 Markdown 格式，结构清晰
2. 第一行必须是 # 标题，格式为 "# 技能名称"
3. 标题下第一行是技能的一句话描述
4. 包含以下章节：适用场景、核心规则、具体示例、禁止事项
5. 写"规则"而不是"教程"——直接告诉 AI 怎么做，不要解释概念
6. 用具体示例代替模糊描述
7. 禁止事项要明确具体，不要用"尽量不要"这种模糊表述
8. 控制在 100-300 行

请只返回 Markdown 内容，不要其他说明。格式如下：

# 技能名称

一句话描述这个技能是做什么的。

## 适用场景

在什么情况下加载这个技能。

## 核心规则

- 规则1
- 规则2
...

## 示例

具体的使用示例。

## 禁止事项

- 禁止1
- 禁止2
..."""
